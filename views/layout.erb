<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <title><%= @title || 'The Exceptionist' %></title>
  <link type="text/css" href="/blueprint.css" rel="stylesheet" media="screen"/>

  <style>
    #project_nav {
      float: right;
      font-weight: bold;
    }

    div.head_box {
      padding: 5px;
      background-color: #eee;
      font-size: 1.5em;
      color: #111;
    }

    dd.backtrace {
      background-color: #0B0F21;
      color: #fff;
      white-space: pre;
      overflow: auto;
      max-height: 250px;
      padding: 5px 10px;
    }

    dd {
      margin-bottom: 10px;
    }

    .centered {
      text-align: center;
    }

    span.occurrence_count {
      background-color: #eee;
      font-size: 1.2em;
      padding: 8px;
      color: #666;
    }
  </style>
</head>

<body>
  <div class="container prepend-top">
    <div id="project_nav">
      <a href="/">Dashboard</a> |
      <select onchange="window.location=this.options[this.selectedIndex].value">
        <% for project in @projects %>
          <option value="/">Dashboard</option>
          <option value="/projects/<%= project.name %>" <%= 'selected="selected"' if @current_project == project %>><%= project.name %></option>
        <% end %>
      </select>
    </div>

    <%= yield %>
  </div>
</body>
</html>
